class AppDurations {
  const AppDurations._();

  static const shortAnimation = Duration(milliseconds: 140);
  static const bookSelectionDuration = shortAnimation;
  static const bookSwap = Duration(milliseconds: 220);
  static const clueStateChange = Duration(milliseconds: 200);
  static const clueCompletionDelay = Duration(milliseconds: 200);
  static const clearBookStep = Duration(milliseconds: 90);
  static const clearFinalGlow = Duration(milliseconds: 300);
  static const resultOverlay = Duration(milliseconds: 240);
}
